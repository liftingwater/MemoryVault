# In production the frontend and API share the same CloudFront domain.
# All API calls are relative URLs (e.g. /health, /api/*) which CloudFront
# routes to API Gateway. No absolute URL is needed or correct here.
VITE_API_BASE_URL=
